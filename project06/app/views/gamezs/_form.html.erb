<%= form_for(@gamez) do |f| %>
  <% if @gamez.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gamez.errors.count, "error") %> prohibited this gamez from being saved:</h2>

      <ul>
      <% @gamez.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :rating %><br />
    <%= f.text_field :rating %>
  </div>
  <div class="field">
    <%= f.label :date_added %><br />
    <%= f.text_field :date_added %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
